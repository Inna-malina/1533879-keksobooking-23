import {
  createDataBase
} from "../js/description.js";

import {
  createCard,
  renderCard,
  renderCards
} from "../js/card.js";



const map = document.querySelector('#mapid');
const data = createDataBase(5);
// createCard();
// renderCard(map, createCard(data[5]));
// renderCards(map, data);
